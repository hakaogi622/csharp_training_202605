@*Confirm.cshtml(社員登録の確認画面)*@

@using WebApp_Src.Presentations.ViewModels
@model EmployeeUpdateViewModel;
@{
    ViewData["Title"] = "社員更新(確認)";
}
<h2 class="text-center mt-4">@ViewData["Title"]</h2>
<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6 col-sm-8">
            <table class="table table-hover">
                <tbody>
                    <tr>
                        @*EmployeeRegisterViewModelのNameプロパティの値を表示する*@
                        <td class="fw-bold">@Html.DisplayNameFor(m => m.EmpName)</td>
                        <td>@Html.DisplayFor(m => m.EmpName)</td>
                    </tr>
                    <tr>
                        @*メールアドレス*@
                        <td class="fw-bold">@Html.DisplayNameFor(m => m.EmpMailadress)</td>
                        <td>@Html.DisplayFor(m => m.EmpMailadress)</td>

                    </tr>
                    <tr>
                        @*電話番号*@
                        <td class="fw-bold">@Html.DisplayNameFor(m => m.EmpPhonenumber)</td>
                        <td>@Html.DisplayFor(m => m.EmpPhonenumber)</td>
                    </tr>
                    <tr>
                        @*EmployeeRegisterViewModelのDeptNameプロパティの値を表示する*@
                        <td class="fw-bold">@Html.DisplayNameFor(m => m.DeptName)</td>
                        <td>@Html.DisplayFor(m => m.DeptName)</td>
                    </tr>
                </tbody>
            </table>

            <form>
                @*POSTリクエスト(Register/Back)に値を渡すためにhidden属性を使う*@
                @*これがないと、次の画面で値が消えてしまう*@
                <input type="hidden" asp-for="EmpId" />
                <input type="hidden" asp-for="EmpName" />
                <input type="hidden" asp-for="EmpMailadress" />
                <input type="hidden" asp-for="EmpPhonenumber" />
                <input type="hidden" asp-for="DeptId" />
                <input type="hidden" asp-for="DeptName" />
                @*formaction/formmethod ボタンごとに送信先を変える*@
                @*同じフォーム内で[登録]と[戻る]を分岐できる*@
                <div class="text-center mt-4">
                    <button type="submit" formaction="@Url.Action("Update", "EmployeeUpdate")" formmethod="post"
                            class="btn btn-primary">登録</button>
                    <button type="submit" formaction="@Url.Action("Back", "EmployeeUpdate")" formmethod="post"
                            class="btn btn-secondary">戻る</button>
                </div>
            </form>
        </div>
    </div>
</div>